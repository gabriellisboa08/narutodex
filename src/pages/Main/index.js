import React from "react";
import CardsList from "../../components/CardsList";

const MainPage = () => {
    return (
        <div>
            <CardsList />
        </div>
    );
};

export default MainPage;
